<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:orientation="vertical"
        android:layout_marginTop="20dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    <View
            android:id="@+id/vLine"
            android:layout_width="match_parent"
            android:layout_height="0.5dp"
            android:background="@color/divider"
            app:layout_constraintTop_toTopOf="parent"
            />

    <ImageView
            android:id="@+id/ivSendImage"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:src="@drawable/ic_image_option"
            android:layout_margin="8dp"
            app:layout_constraintTop_toBottomOf="@id/vLine"
            app:layout_constraintStart_toStartOf="parent"
            />

    <EditText
            android:id="@+id/etMessageField"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:background="@drawable/selector_message_box"
            android:hint="Type Message Here"
            android:textAppearance="@style/CustomTextAppearance"
            android:padding="8dp"
            android:layout_margin="8dp"
            app:layout_constraintTop_toBottomOf="@id/vLine"
            app:layout_constraintStart_toEndOf="@id/ivSendImage"
            app:layout_constraintEnd_toStartOf="@id/tvSendMessage"
            />

    <ImageView
            android:id="@+id/tvSendMessage"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:src="@drawable/ic_send_message"
            android:layout_margin="8dp"
            app:layout_constraintTop_toBottomOf="@id/vLine"
            app:layout_constraintEnd_toEndOf="parent"
            />

</androidx.constraintlayout.widget.ConstraintLayout>